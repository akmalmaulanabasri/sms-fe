import Box from '@mui/material/Box'
import Typography from '@mui/material/Typography'

const RppPage = () => (
  <Box sx={{ p: 6 }}>
    <Typography variant='h4' sx={{ mb: 3 }}>
      RPP Digital
    </Typography>
    <Typography>Upload and preview RPP documents (placeholder).</Typography>
  </Box>
)

export default RppPage
